// Maintenance only: regenerate the committed, script-free worksheet after source edits.
const fs = require('node:fs');
const path = require('node:path');
const { sources, task } = require('./context.js');
const escape = text => text.replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;');
const html = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Exercise 2 · Source cards and worksheet</title><link rel="stylesheet" href="../../../shared/theme.css"><link rel="stylesheet" href="../../../shared/styles.css"><link rel="stylesheet" href="styles.css"></head><body><main class="worksheet">
<p class="print-hide"><a class="home-link" href="index.html">← Context Lab</a></p><div class="eyebrow">EXERCISE 02 / OFFLINE WORKSHEET / V1.0</div><h1>Build a context package</h1><p>Fictional password-reset teaching evidence. Work individually or in pairs. Allow 2 minutes for instructions, 6 for work, 3 for review and 1 for transition.</p><p>Choose Include, Exclude or Defer for each source. Explain at least two exclusion/defer decisions, resolve the expiry conflict, and record two unanswered requirements questions. Include the IDs and excerpts you would supply. Diagnose CI later.</p><div class="fixed"><strong>Fixed request</strong><p>${escape(task)}</p></div><p class="print-hide">Print this page or record your answers in a local text document. These source cards work without JavaScript or a network connection. Open the worked example only after your first decisions.</p>
${sources.map(s => `<section class="source"><h2>${escape(s.id)} · ${escape(s.title)}</h2><p>${escape(s.meta)}</p><p class="source-text">${escape(s.text)}</p><p>Decision: □ Include now &nbsp; □ Exclude &nbsp; □ Defer</p>${s.id === 'OLD-01' ? '<p>If included, state how this source should be used.</p>' : ''}<p>Rationale:</p><div class="answer-space"></div></section>`).join('\n')}
<section><h2>Your context package</h2><p>Included source IDs and excerpts (refer to the cards above):</p><div class="answer-space"></div><p>Expiry conflict: which rule governs, and why?</p><div class="answer-space"></div><p>Unresolved question 1:</p><div class="answer-space"></div><p>Unresolved question 2:</p><div class="answer-space"></div><h2>Review and revise</h2><p>Compare with the worked example. Which evidence changed your decision? Record one revision or explain why you kept your package.</p><div class="answer-space"></div><p>What improved the instructions, and what improved their evidential basis? What remains unknown?</p><a class="home-link" href="worked-example.html">Worked example and review guidance</a></section></main></body></html>\n`;
fs.writeFileSync(path.join(__dirname, 'worksheet.html'), html);
