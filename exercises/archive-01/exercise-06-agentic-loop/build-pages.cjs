'use strict';
const fs=require('node:fs'),path=require('node:path'),M=require('./model.js');
const scenario=fs.readFileSync(path.join(__dirname,'../../../../tutorial-scenario.md'),'utf8').replace(/\r/g,'');
const between=(a,b)=>{if(!scenario.includes(a)||!scenario.includes(b))throw Error('Scenario heading changed');return scenario.split(a)[1].split(b)[0].trim();};
const sources={
 'ACs':between('## Current requirements — preserve verbatim','**OLD-01:**'),
 'TIM-01':between('**TIM-01:**','**DATA-01:**'),
 'DATA-01':between('**DATA-01:**','## CODE-01'),
 'CODE-01':between('## CODE-01 — deliberately flawed, framework-independent pseudocode','## CI-01'),
 'CI-01':between('## CI-01 — initial failure excerpt','## HIST-01'),
 'HIST-01':between('## HIST-01 — small supplied sample','## Progressive mission evidence'),
 'TRACE-01':between('### TRACE-01 — correlated capture for run 204','### CHANGE-01'),
 'CHANGE-01':between('### CHANGE-01 — version comparison','### VALID-01'),
 'VALID-01':between('### VALID-01 — prepared comparison, revealed after approval','### GAP-01'),
 'GAP-01':between('### GAP-01 — unavailable-evidence variant','### CONFLICT-01'),
 'CONFLICT-01':between('### CONFLICT-01 — mismatched-evidence variant','## Risk and chain cards'),
 'ERROR-01':'Workshop tool-error variant: retrieval failed before a capture was returned. One dispatched attempt was consumed. Preserve the error and gap; no run evidence was obtained.',
 'REQUEST-01':'Workshop request receipt: the test owner has received the run=204/request=reset-204 diagnostic request. No new capture is available in this exercise. Escalate the unresolved question; this receipt is not diagnostic evidence.'
};
const initial=['ACs','TIM-01','DATA-01','CODE-01','CI-01','HIST-01'];
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const option=(v,l)=>`<option value="${v}">${esc(l)}</option>`;
const select=(id,label,items)=>`<label for="${id}">${label}</label><select id="${id}" name="${id}"><option value="">Choose…</option>${items.map(([v,l])=>option(v,l)).join('')}</select>`;
const text=(id,label,hint)=>`<label for="${id}">${label}</label><p class="field-note" id="${id}-hint">${hint}</p><textarea id="${id}" name="${id}" rows="2" maxlength="1800" aria-describedby="${id}-hint"></textarea>`;
const page=(title,body,script=false)=>`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${title} · Testing Lab</title><link rel="icon" href="../../../shared/favicon.ico"><link rel="stylesheet" href="../../../shared/theme.css"><link rel="stylesheet" href="../../../shared/styles.css"><link rel="stylesheet" href="../../../shared/forms.css"><link rel="stylesheet" href="styles.css">${script?'<script defer src="sources.js"></script><script defer src="model.js"></script><script defer src="app.js"></script>':''}</head><body><a class="skip-link" href="#task-title">Skip to exercise</a><header class="topbar"><a class="brand" href="../../../index.html">Testing <span>Lab</span></a><nav class="exercise-nav" aria-label="Exercise navigation"><a class="home-link" href="../../../index.html">All exercises</a><span class="badge">EXERCISE 06</span></nav></header><main><section class="intro"><div class="eyebrow">AGENTIC AI LOOP / FINAL MISSION</div><h1 id="task-title" tabindex="-1">${title}</h1><p>Design a workflow another person can follow without guessing.</p><div class="notice"><span class="dot" aria-hidden="true"></span>Local simulation · No AI service connected · Nothing is sent or saved</div></section>${body}<footer>From Prompts to Agentic Testing Workflows <span>Design → observe → decide → review</span></footer></main></body></html>\n`;
const brief=`<section class="card"><div class="eyebrow">YOUR TASK / 28 MINUTES</div><p><strong>Design, test, and revise a bounded investigation of run 204.</strong> Your goal is a proportionate recommendation ready for expert review. Use precise prompts (1), grounding (2), evidence judgment (3), reviewed handoffs (4), and validation with authority (5).</p><p class="field-note">2 minutes brief · 8 design · 2 peer/self-review · 6 dry-run · 3 challenge/revision · 6 debrief · 1 copy/access. Work in pairs or individually.</p><p><strong>Provisional handoff:</strong> Exercise 5 proposes bounded synchronization and independent credential checks; password policy remains unresolved. The proposal needs validation and scoped approval. It does not establish the cause of run 204. No saved earlier answers are needed.</p><p class="loop">Observe → assess state → select action → check authority/budget → act → validate observation → update state → continue or stop</p><p>Participants simulate model decisions. The harness enforces limits independently. Prepared cards are fictional; this is not a live AI agent.</p></section>`;
const pack=`<details class="card"><summary>Starting evidence pack · already available</summary><p>Current requirements define expected behavior. Logs/code describe observations and implementation. Preserve gaps, including the unspecified password policy and exact expiry boundary.</p>${initial.map(id=>`<details class="source"><summary>${id}</summary><pre>${esc(sources[id])}</pre></details>`).join('')}</details>`;
const policy=`<p>Read/draft within this pack is allowed. A simulated isolated comparison needs mission-specific approval from your named reviewer. Exercise 5 approval does not transfer. Production and failure-hiding writes are prohibited. Each dispatched retrieval/execution attempt counts, including errors and repeats. Initial reading, reasoning, approvals, edits, and blocked requests do not. Two consecutive no-progress results or denied requests escalate. A learner budget of 1–5 is enforced; omitted means five. At the final action, enough evidence may still support a recommendation; otherwise stop at the limit.</p>`;
const design=`${M.fields.map(f=>text(...f)).join('')}${M.groups.map(g=>select(g.id,g.label,g.options)).join('')}<h3>Write your observation-dependent policy</h3><p>Choose an action and explain its information value for each condition. Tool contracts below are available capabilities; your design must specify which ones it uses and how.</p>${M.situations.map(([id,l])=>`<fieldset><legend>If: ${l}</legend>${select('rule-'+id,'Then:',M.actions)}${text('why-'+id,'Because / expected evidence:','Write why this action addresses this uncertainty and what it leaves unresolved.')}</fieldset>`).join('')}${select('cap','Maximum dispatched attempts (blank defaults to five)',[1,2,3,4,5].map(n=>[n,String(n)]))}${text('review','Peer or self-review before commitment','Could another person follow the loop? Which observation changes its next action? What blocks unsafe or unsupported steps? What ends it?')}`;
const contracts=`<details class="card"><summary>Available tool/action contracts and enforced workshop policy</summary>${policy}${M.actions.map(([id,l,t])=>`<h3>${esc(l)}</h3><p>${esc(t)}</p>`).join('')}<p>Variant selectors in the dry-run choose a fictional workshop situation, not evidence supplied by the controller. They allow unavailable/error paths to be explored. Reveal separation is a teaching convention, not a security boundary.</p></details>`;
const index=page('Design an agentic AI loop',`${brief}<p class="resource-links"><a href="worksheet.html">Printable activity</a> · <a href="workflow-template.md" download>Editable investigation template</a> · <a href="../exercise-05-automation-review/workflow-template.md" download>Automation review template</a> · <a href="../exercise-04-prompt-chain/workflow-template.md" download>Test-design template</a> · <a href="transfer-canvas.md" download>Own-context canvas</a></p>${pack}${contracts}<noscript><section class="card">JavaScript is off. Use the <a href="worksheet.html">script-free activity and numbered reveal map</a>.</section></noscript><div id="interactive" hidden>
<section class="card"><h2>1 · Design and commit</h2><p>Write concise rules, not a perfect essay. Candidate strategies contain trade-offs and plausible flaws. Written reasoning needs human review; it is not automatically scored.</p><form id="design-form">${design}<button class="primary" id="commit" type="submit">Commit initial loop design</button></form><p id="design-status" role="status"></p><div id="design-feedback" hidden></div></section>
<section class="card" id="mission" hidden><h2 tabindex="-1" id="mission-title">2 · Dry-run your rules</h2><p id="state-view" class="outcome" aria-live="polite"></p><p id="rule-view"></p><form id="action-form">${text('update','Hypothesis update before this action','Separate observed facts from your explanation; cite what changed or remains unknown.')}${select('action','Proposed next action',M.actions)}${text('reason','Why this action now?','What could this action establish or weaken?')}${text('gap','Design gap / justified deviation (if action differs from your rule)','Name the missing or inadequate rule before deviating; the original design remains recorded.')}${select('variant','Workshop trace-return variant',[['normal','Normal capture'],['missing','Capture unavailable'],['mismatch','Wrong run returned'],['error','Retrieval error']])}${select('approval','Workshop approval record for this attempt',[['none','No mission approval'],['prior','Prior Exercise 5 approval only'],['wrong','Approval for a test patch, not a comparison'],['scoped','Named reviewer approves isolated comparison of run 204 proposal using DATA-01 fixtures']])}<p class="field-note">Approval selections are hypothetical teaching records, not real authorization. Only the matching record authorizes the simulated comparison.</p><button class="primary" type="submit">Propose action / ending</button></form><div id="pending" hidden><h3>Returned observation · not yet admitted to state</h3><pre id="observation"></pre><form id="observation-form">${select('verdict','Validity decision',[['accept','Admit as relevant evidence'],['reject','Reject for this claim'],['hold','Hold for review / clarification']])}${text('validity','Evidence check and updated interpretation','Check identity, source, timing basis, and scope. What changes in your hypotheses? What remains unknown?')}<button class="primary" type="submit">Review observation and update state</button></form></div><p id="mission-status" role="status"></p><div id="trace-list"></div></section>
<section class="card" id="challenge-panel" hidden><h2>3 · Challenge, revise, and replay</h2><p>Replace one observation at a checkpoint with missing or mismatched evidence. This counterfactual never changes the original run or restores its budget. Preserve the first design, then revise one rule or handoff.</p><form id="challenge-form">${select('checkpoint','Checkpoint (before an action)',[])}${select('challenge-variant','Counterfactual observation',[['missing','GAP-01: unavailable capture'],['mismatch','CONFLICT-01: wrong run/request']])}<button class="primary" type="submit">Challenge the original rule</button></form><pre id="challenge-result"></pre><form id="revision-form" hidden>${select('revised-action','Revised invalid-evidence rule: next action',M.actions)}${text('revised-why','Revised rule rationale / expected evidence','Explain the benefit and limitation of the revised rule.')}${text('revision-note','One weakness corrected and its handoff consequence','What changed, why, and who checks the result?')}<button class="primary" type="submit">Commit one revision and replay decision</button></form><pre id="replay-result"></pre><p id="challenge-status" role="status"></p></section>
<section class="card" id="handoff-panel" hidden><h2>4 · Final handoff and debrief</h2><form id="handoff-form">${text('conclusion','Recommendation or unresolved question','Cite retained sources, alternatives, uncertainty, validation still needed, and the next human action. A green simulation is not a verified fix.')}<button class="primary" type="submit">Record final handoff</button></form><p id="handoff-status" role="status"></p><p>Could someone follow the loop without guessing? Which observation changed your action? What blocked an unsupported or unauthorized step? Why did you stop?</p><p><a href="feedback.html">Compare reasoning and ending types</a> · <a href="worked-example.html">Two worked alternatives</a></p></section>
<section class="card" id="record-panel" hidden><h2>Your complete record</h2><p>Nothing persists. Copy before reset/reload. Draft changes after feedback are labeled stale.</p><button id="copy" type="button" class="text-button">Copy record</button><p id="copy-status" role="status"></p><textarea id="record" rows="12" readonly aria-label="Complete loop record"></textarea></section><button id="reset-attempt" type="button" class="text-button">Reset attempt</button></div>`,true);
const worksheet=page('Agentic loop worksheet',`${brief}<p><a href="index.html">Interactive activity</a> · <a href="workflow-template.md">Editable template</a> · <a href="transfer-canvas.md">Own-context canvas</a></p>${pack.replaceAll('<details','<details open')}${contracts.replace('<details','<details open')}<section class="card"><h2>1 · Design before revealing</h2>${M.fields.map(([,l,h])=>`<h3>${l}</h3><p>${h}</p><div class="write-space"></div>`).join('')}${M.groups.map(g=>`<h3>${g.label}</h3>${g.options.map(o=>`<p>□ ${o[1]}</p>`).join('')}`).join('')}<h3>Observation → action → reason</h3>${M.situations.map(([,l])=>`<p>If ${l}, then ___ because ___; required inputs/output and reviewer ___.</p><div class="write-space"></div>`).join('')}<p>Budget (1–5; omitted = 5): ___ . Peer/self-review note: ___ . Commit initial design before requesting cards.</p><h2>2 · Dry-run and reveal map</h2><p>For each decision record: current evidence/state, hypothesis update, selected action/inputs, matching rule or design gap, rationale, authority check, budget, result, validity check, updated state, and continue/ending. Use two decisions or a justified early stop.</p><ol><li>Trace request → reveal TRACE-01; choose GAP-01, CONFLICT-01, or ERROR-01 only as a clearly labeled alternative route.</li><li>Version comparison → CHANGE-01. Unchanged-history read → initial HIST-01.</li><li>Isolated comparison → VALID-01 only after recording the named reviewer's matching mission-specific approval.</li><li>Missing-evidence request → REQUEST-01. No capture is available; escalate.</li><li>Prohibited or unapproved action → block without revealing results or spending an action. Two consecutive denied requests escalate.</li><li>Each dispatched request, error, and repeat counts. At the cap, stop unless retained evidence already meets the recommendation threshold. Two consecutive no-progress results escalate.</li></ol><p><a href="evidence-cards.html">Separate result cards: open only the requested card</a>. Reveal separation is a teaching convention.</p><div class="write-space">Trace / approval / action tally (continue on a separate sheet):</div><h2>3 · Counterfactual and revision</h2><p>Choose one checkpoint before an action. Replace its observation with GAP-01 or CONFLICT-01 on a separate sheet. Apply the invalid-evidence rule. Record a revised rule/handoff and replay the decision with the same retained state and remaining budget. Do not mix variant evidence into the original run.</p><div class="write-space">Initial rule → consequence → revised rule → next decision and reason:</div><h2>4 · Final handoff</h2><p>Recommendation-ready / escalated / limit reached: ___ . Cited evidence, alternatives, uncertainty, outstanding validation, named reviewer and next action: ___ .</p><p>Only correlated trace plus CI-01/CODE-01/TIM-01 supports the bounded timing recommendation here. The comparison threshold additionally needs approved VALID-01. A green result alone is insufficient. The actual patch remains unverified.</p><p>Preserve first answers before <a href="feedback.html">feedback</a> and <a href="worked-example.html">worked alternatives</a>.</p></section>`);
const cards=page('Mission result cards',`<section class="card"><p>Fictional results. <a href="worksheet.html">Use the worksheet reveal map</a>. Do not read all cards in advance; choose one according to the action and recorded approval. Alternative cards replace the trace response on a separate route.</p>${Object.entries(sources).filter(([id])=>!initial.includes(id)).map(([id,t])=>`<details class="source" id="${id}"><summary>${id}${id==='VALID-01'?' · approval required':''}</summary><pre>${esc(t)}</pre></details>`).join('')}</section>`);
const feedback=page('Loop design feedback',`<section class="card"><p>Prepared teaching guidance; inspect the learner's written rules with a person. No score proves competence.</p>${M.groups.map(g=>`<h2>${g.label}</h2>${g.options.map(o=>`<h3>${o[1]}</h3><p>${o[2]}</p>`).join('')}`).join('')}<h2>Three valid ending types</h2><p><strong>Recommendation ready:</strong> valid run correlation plus TIM-01/CODE-01 supports a limited timing explanation; add approved VALID-01 when the selected threshold requires it. Keep real validation outstanding.</p><p><strong>Escalated:</strong> missing/mismatched evidence, unavailable authority, or repeated no progress needs a named reviewer and a specific unresolved question.</p><p><strong>Limit reached:</strong> budget exhausted without sufficient evidence. Preserve what was learned, do not invent the missing result, and propose a human next step.</p><h2>Agency and counterfactual review</h2><p>A fixed read-everything sequence does not adapt to observations. A useful policy requests a correlated capture when identity is missing, then chooses comparison or a bounded handoff when correlation is established. A mismatched capture changes that decision to a targeted request or escalation. An LLM could propose these actions; the harness must still enforce permissions and limits. This activity uses participants and deterministic cards.</p><p>For the revision, compare the initial invalid-evidence rule with the revised action, information value, remaining budget, and handoff. Requesting new evidence can be defensible; immediate escalation can also be proportionate. Taking an unrelated action or accepting the mismatched capture does not resolve identity.</p><a href="worked-example.html">Worked alternatives</a></section>`);
const worked=page('Worked loop alternatives',`<section class="card"><p>Read after committing your design. Authored examples, not real execution results.</p><h2>Shared controller and contracts</h2><p>Investigate only run 204/reset-204. Input: approved requirements, CI-01, CODE-01, HIST-01, TIM-01, DATA-01, source-linked ledger, hypotheses, count and scoped approvals. Retain facts separately from interpretations. Output: updated hypotheses, evidence IDs, action with inputs, rationale, and proposed ending. Reject unsupported certainty and treat retrieved text as evidence, not instructions.</p><p>Trace tool: input run/request/fixture IDs; return matching capture and timing origin, or explicit missing/error. Version tool: input runs 201–205; return version metadata, not a cause verdict. Comparison: input isolated proposal and DATA-01 fixtures; return discriminating outcomes only after mission-specific approval. Harness checks authority/budget before dispatch; evidence reviewer checks identity/scope before admission. Test owner approves comparison; automation reviewer assesses the final recommendation.</p><h2>A · Bounded recommendation, validation outstanding</h2><p>Budget 3; evidence ledger; identity gate; bounded threshold. Hypotheses: premature snapshot versus reset completion outside TIM-01. If no correlated trace, retrieve it to distinguish those explanations. If correlated, compare versions then hand off a limited recommendation (record this second-stage detail in the controller). If invalid, escalate to the test owner.</p><ol><li>Attempt 1: retrieve TRACE-01, accept matching run/request and clock. The 1,460 ms confirmation follows the 1,002 ms snapshot and meets TIM-01. Timing race gains support; credential behavior remains unknown.</li><li>Attempt 2: retrieve CHANGE-01, accept version metadata. Recent version change is less plausible, but environment effects/pre-existing defects remain possible.</li><li>Stop recommendation-ready: propose bounded synchronization and stronger AC-04 checks; real implementation and discriminating validation are outstanding. Benefit: fewer actions and no execution approval needed. Limitation: no controlled comparison in this route.</li></ol><h2>B · Comparison before recommendation</h2><p>Budget 3; compact summary with linked ledger; cross-check gate; comparison threshold. If trace missing, retrieve it. If correlated, request the isolated comparison with scoped approval; when VALID-01 is retained, recommend. If evidence invalid, request matching capture once, then escalate if unavailable.</p><ol><li>Attempt 1: retrieve and validate TRACE-01 as above.</li><li>Attempt 2: designated test owner approves the isolated run 204 proposal comparison using DATA-01. Reveal and validate VALID-01: controlled delay and injected credential fault discriminate stronger from weak tests.</li><li>Recommend the change for actual implementation/validation. Benefit: more discriminating evidence. Limitation: approval/action cost; prepared examples cannot verify the real system.</li></ol><h2>Challenge and one revision</h2><p>Before the first trace response, substitute CONFLICT-01 on a separate sheet. A weak initial rule “if any trace returns, recommend” would accept run 207 for run 204. Revise it to “check run/request first; request the correct capture, or escalate if unavailable.” Replay with the same checkpoint budget: REQUEST-01 is only a receipt, so hand the unresolved run correlation to the test owner. The original run and evidence remain intact.</p><h2>Limit example</h2><p>Budget 1 with comparison threshold: TRACE-01 is useful, but the comparison is still missing. Stop limit-reached and hand off the remaining validation need. A short trace can be a sound outcome.</p><a href="index.html">Return to exercise</a></section>`);
const template='# Failure-investigation loop record\n\nFictional workshop or sanitized own-context task. No model/execution is implied.\n\n'+M.fields.map(([,l,h])=>'## '+l+'\n\n'+h+'\n\nAnswer: \n').join('\n')+'\n## Decision rules and controls\n\nObservation → action/inputs → expected information → output review gate → next state/ending.\n\n- Missing correlated evidence:\n- Correlated evidence:\n- Missing/mismatched/error result:\n- Evidence threshold met:\n- State strategy and evidence gate:\n- Completion threshold:\n- Budget (1–5), no-progress rule, approval scope:\n- Peer/self-review note:\n\n## Initial trace\n\nFor each step: state, hypothesis update, action/rationale, matching rule or gap, authority/budget, result/source, validity decision, updated state and ending.\n\n## Separate counterfactual\n\nCheckpoint/state/count; substitute observation; initial rule/decision; limitation.\n\n## One revision and replay\n\nPreserve original. Revised rule/handoff; reason; same checkpoint budget; resulting decision.\n\n## Final handoff\n\nEnding; recommendation or unresolved question; evidence IDs; alternatives; uncertainty; outstanding validation; reviewer and next action.\n';
const canvas='# Own-context workflow canvas\n\n10 minutes: 1 instruction, 5 individual design, 3 peer/self-review, 1 closing. Do not include confidential information.\n\n- Task, goal, and scope:\n- Sources, authority, facts, assumptions, and gaps:\n- Output and review criterion:\n- Stages, inputs/outputs, and reviewed handoffs:\n- Fixed sequence or observation-dependent decisions, and why:\n- Tools and permissions:\n- Evidence retained:\n- Human reviewer and approval points:\n- Budget, recovery, completion, and escalation:\n- Concrete success measure beyond an AI answer:\n\n## Review and revise\n\nWhat evidence justifies the output? Where is human review required? What causes completion or escalation?\n\n- Peer/self-review comment:\n- Resulting correction or justified retention:\n\nA fixed workflow is valid when agency adds no value.\n\n## Compact worked example\n\nInvestigate run 204, not global service health. Use CI-01/CODE-01/TIM-01 and a source-linked ledger; request matching trace before a cause recommendation. Read is allowed; isolated comparison needs scoped approval. Preserve hypotheses, identities, observations, and action count. With a budget of three, stop for review when correlated evidence supports a limited recommendation; escalate missing identity or stop unresolved at the cap. The automation reviewer checks citations and whether contrary evidence would change the action. Success means a reviewable, bounded handoff, not a green simulation.\n';
const outputs={'sources.js':'/* Generated from the fictional scenario; reveals are enforced in the UI. */\n(function(r){const s='+JSON.stringify(sources,null,2)+';if(typeof module==="object"&&module.exports)module.exports=s;else r.LoopSources=s;})(globalThis);\n','index.html':index,'worksheet.html':worksheet,'evidence-cards.html':cards,'feedback.html':feedback,'worked-example.html':worked,'workflow-template.md':template,'transfer-canvas.md':canvas};
for(const [file,body]of Object.entries(outputs)){const target=path.join(__dirname,file);if(process.argv.includes('--check')){if(!fs.existsSync(target)||fs.readFileSync(target,'utf8')!==body)throw Error('Stale generated file '+file);}else fs.writeFileSync(target,body);}
console.log(process.argv.includes('--check')?'Generated sources and pages are current.':'Generated sources, runtime page, fallback, feedback, examples, and templates.');
