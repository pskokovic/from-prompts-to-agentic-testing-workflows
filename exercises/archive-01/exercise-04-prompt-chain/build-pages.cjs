'use strict';
const fs=require('node:fs'),path=require('node:path'),M=require('./model');const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const sources=`<ul>${Object.entries(M.requirements).map(([id,t])=>`<li><strong>${id}</strong>: ${t}</li>`).join('')}</ul><p><strong>Known gaps:</strong> ${M.gaps}</p>`;
function page(title,body,scripts=''){return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${title} · Testing Lab</title><link rel="icon" href="../../../shared/favicon.ico"><link rel="stylesheet" href="../../../shared/theme.css"><link rel="stylesheet" href="../../../shared/styles.css"><link rel="stylesheet" href="../../../shared/forms.css"><link rel="stylesheet" href="styles.css">${scripts}</head><body><a class="skip-link" href="#title">Skip to exercise</a><header class="topbar"><a class="brand" href="../../../index.html">Testing <span>Lab</span></a><nav class="exercise-nav" aria-label="Exercise navigation"><a class="home-link" href="../../../index.html">All exercises</a><span class="badge">EXERCISE 04</span></nav></header><main><section class="intro"><div class="eyebrow">BUILD · OBSERVE · IMPROVE</div><h1 id="title" tabindex="-1">${title}</h1><p>Choose what deserves to become the next prompt’s input.</p><div class="notice"><span class="dot" aria-hidden="true"></span>Local simulation · No AI service connected · Nothing is sent or saved</div></section>${body}<footer>From Prompts to Agentic Testing Workflows <span>Reviewed output → next prompt</span></footer></main></body></html>`;}
const brief=`<section class="card"><h2>Your goal</h2><p>Build a small password-reset regression proposal that prioritizes <strong>credential protection</strong>, uses supported expectations, and makes automation dependencies explicit.</p><p>Use at most three test ideas. Setup capabilities are unconfirmed. Broader coverage may be useful but requires more setup and review.</p><p class="field-note">16 minutes: 2 instruction · 5 build · 2 observe · 2 revise · 4 debrief · 1 transition. Prepared outputs and fictional examples; no AI or tests execute. Work is held in page memory only. Copy before leaving.</p><div class="actions"><a href="worksheet.html">Worksheet</a><a href="fallback.html#path-start">Script-free branching activity</a><a href="workflow-template.md" download>Editable template</a></div></section>`;
function history(p){let s=M.initial();return p.map((id,i)=>{const o=M.options(i,s).find(x=>x.id===id);s=M.apply(s,i,id);return `<li><strong>${M.stages[i]} · ${esc(o.title)}</strong><p>${esc(o.text)}</p></li>`;}).join('');}
function results(p,stop=false){const r=M.evaluate(p,stop);return `<h2>${r.status}</h2><p>${r.next}</p><dl>${r.profile.map(([a,b])=>`<dt>${a}</dt><dd>${esc(b)}</dd>`).join('')}</dl><h3>Prepared consequences — no execution</h3>${r.cases.map(c=>`<section class="outcome"><h4>${c.id}</h4><p>${c.observed}</p><p><strong>${c.verdict}</strong></p></section>`).join('')}<p>${esc(r.tradeoff)}</p><h3>Why this assessment</h3><p>${esc(r.rankingReason)}</p>${r.corrections.length?'<h3>Corrections made along your path</h3>'+r.corrections.map(c=>`<p>${esc(c)}</p>`).join(''):''}<h3>Why each choice matters</h3>${r.explanation.map(e=>`<details class="stage-explanation"><summary>${M.stages[e.stage]} · ${esc(e.title)}</summary><dl>${[['Selected output',e.selected],['Why it is supported or flawed',e.why],['Benefit',e.benefit],['Limitation',e.limitation],['Downstream effect',e.effect],['Later outcome',e.outcome],['When an alternative helps',e.alternative]].map(([a,b])=>`<dt>${a}</dt><dd>${esc(b)}</dd>`).join('')}</dl>${e.corrections.map(c=>`<p>${esc(c)}</p>`).join('')}</details>`).join('')}<p>Source fidelity and credential faults cannot be compensated for by convenience. Equivalent defensible paths may tie.</p>`;}
const index=page('Build a prompt-chain path',`${brief}<details class="card" open><summary>Starting sources — requirements and known gaps</summary>${sources}</details><noscript><p class="card">Use the <a href="fallback.html#path-start">script-free branching activity</a> or the worksheet with your facilitator.</p></noscript><div id="interactive" hidden><section class="card"><h2>Your selected path</h2><p>Use a stage button to reconsider it. Passing a replacement clears later choices so you can review newly generated outputs.</p><ol id="path"></ol><p id="path-status" role="status"></p></section><section id="stage-panel" class="card"><div id="stage-counter" class="eyebrow"></div><h2 id="stage-title" tabindex="-1"></h2><details open><summary>Actual input to this prompt</summary><div id="input"></div></details><h3>Prompt</h3><p id="prompt"></p><fieldset><legend>Choose one prepared output (required)</legend><div id="options" class="options"></div></fieldset><p id="choice-help" role="status">Choose an output before continuing. It need not be correct.</p><button id="next" class="primary" type="button" disabled aria-describedby="choice-help">Pass selected output to next stage</button></section><section class="card"><h2>Review your proposal</h2><p>Finish the six stages to compare consequences, or explicitly stop to request review. A stopped proposal is not approved.</p><label>Your judgment or reason for stopping (required)<textarea id="reason" rows="2" maxlength="1400"></textarea></label><div class="actions"><button id="finish" class="primary" type="button" disabled>Inspect consequences</button><button id="stop" class="text-button" type="button" disabled>Stop for review</button></div><p id="finish-help" role="status"></p></section><section id="results" class="card" hidden><h2 id="result-title" tabindex="-1"></h2><p>These are authored consequences of your proposed checks. No application or CI test ran. Written reasoning needs human review.</p><div id="assessment"></div><div id="comparison"></div><p><a href="worked-example.html">Compare with a reference and a defensible alternative</a></p><p>Revise one upstream choice, review the regenerated later outputs, then inspect consequences again.</p></section><section class="card"><h2>Your decision record</h2><div class="actions"><button id="copy" class="text-button" type="button">Copy record</button><button id="reset" class="text-button" type="button">Reset attempt</button></div><p id="copy-status" role="status"></p><pre id="record" tabindex="0"></pre><p>Debrief: What did the next prompt receive? What changed because of your choice? Which trade-off is defensible for this goal?</p></section></div>`,`<script defer src="model.js"></script><script defer src="app.js"></script>`);
const worksheet=page('Prompt-chain path worksheet',`${brief}<section class="card">${sources}<h2>Build your path</h2><p>Follow the <a href="fallback.html#path-start">branching activity</a> to receive the actual options for your selected input. Your facilitator can print each current branch. Do not reuse a downstream sheet from a different path.</p>${M.stages.map((s,i)=>`<section class="blank"><h3>${i+1}. ${s}</h3><p>Selected input and source IDs: __________________________________</p><p>Chosen output and review note: _________________________________</p></section>`).join('')}<h2>Observe and revise</h2><p>First path / decision: _________________________________________</p><p>Supported or missed consequence: _______________________________</p><p>Trade-off and limitation: ______________________________________</p><p>Revised stage / downstream changes: ____________________________</p><p>Final decision or stop reason: _________________________________</p></section>`);
let branches=[];function walk(p){const id='path-'+(p.join('-')||'start'),s=M.stateFor(p),i=p.length;const previous=p.length?`<a href="#path-${p.slice(0,-1).join('-')||'start'}">Return to previous choice (later outputs must be reviewed again)</a>`:'';let body=i<6?`<h2>${i+1}. ${M.stages[i]}</h2><p><strong>Input:</strong> the selected path below plus the authoritative sources.</p><ol>${history(p)}</ol><p><strong>Prompt:</strong> ${M.prompts[i]}</p><p>Choose one output to pass onward. Record your choice on the worksheet.</p>${M.options(i,s).map(o=>`<article class="fallback-option"><h3>${esc(o.title)}</h3><p>${esc(o.text)}</p><a href="#path-${[...p,o.id].join('-')}">Pass this output</a></article>`).join('')}<p>To stop, write a reason and follow <a href="#stop-${p.join('-')||'start'}">stopped-path feedback</a>.</p>`:`<h2>Your completed path</h2><ol>${history(p)}</ol><p>Record your judgment before inspecting this prepared feedback.</p><details><summary>Reveal consequences and review guidance</summary>${results(p)}<a href="worked-example.html">Worked paths</a></details>`;branches.push(`<section id="${id}" class="route card">${body}<p>${previous}</p></section>`);if(i<6){branches.push(`<section id="stop-${p.join('-')||'start'}" class="route card">${results(p,true)}<p>Copy your selected outputs from the preceding branch and record why you stopped.</p><a href="#${id}">Return to this input</a></section>`);for(const o of M.options(i,s))walk([...p,o.id]);}}walk([]);
const fallback=page('Script-free path builder',`<p class="fallback-help">Use the links to follow your selected outputs. Print only the current branch; record the first path and revisions on your worksheet. <a href="#path-start">Start / reset route</a> · <a href="worksheet.html">Worksheet and sources</a></p>${branches.join('')}`);
const worked=page('Reference and alternative paths',`<section class="card"><p>Compare after your own attempt. This reference is strongest for the stated credential-protection focus, not a universal optimum. Equivalent paths may tie. Prepared examples are not actual test results.</p><h2>Focused reference</h2><ol>${history(M.reference)}</ol>${results(M.reference)}<h2>Broader defensible alternative</h2><ol>${history(M.alternative)}</ol>${results(M.alternative)}<p>Extra replay and expiry checks add coverage, token-state setup, and time-control maintenance. They are not wrong merely because the focused reference uses one idea. AC-01 remains omitted; instruction clarity remains human-led exploration. Policy and exact-boundary gaps remain questions.</p><a href="index.html">Back to exercise</a></section>`);
const template='# Prompt-chain workflow template\n\nGoal and constraints:\nAuthoritative sources and known gaps:\n\n'+M.stages.map((s,i)=>`## ${s}\nPrompt: ${M.prompts[i]}\nActual reviewed input:\nSelected output / rationale:\nSource IDs and unresolved issues:\nReview condition / next-stage handoff:\n`).join('\n')+'\n## Proposal (up to three test ideas)\n\n| Source / risk | Test and observable oracle | Priority / rationale | Data / setup | Automation conditions |\n| --- | --- | --- | --- | --- |\n| | | | | |\n| | | | | |\n| | | | | |\n\nOmitted coverage / duplicates removed:\nHuman-led work:\nFirst judgment / consequences:\nRevision / downstream review:\nFinal limits / implementation-review decision:\n';
const files={'index.html':index,'worksheet.html':worksheet,'fallback.html':fallback,'worked-example.html':worked,'workflow-template.md':template};for(const [file,content]of Object.entries(files)){const target=path.join(__dirname,file);if(process.argv.includes('--check')){if(fs.readFileSync(target,'utf8')!==content)throw Error('Stale '+file);}else fs.writeFileSync(target,content);}console.log('Generated/checkable branching sections: '+branches.length);
